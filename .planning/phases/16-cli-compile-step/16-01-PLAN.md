---
phase: 16-cli-compile-step
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/cli/cli.ts
  - packages/cli/commands/compile.ts
  - packages/cli/cli.test.ts
  - packages/cli/fixtures/self-reflection.logic.md
autonomous: true
requirements: [CLIU-01, CLIU-02]

must_haves:
  truths:
    - "Running `logic-md compile file.logic.md --step analyze` outputs only the compiled step JSON for 'analyze'"
    - "When --step targets a step with self-reflection enabled, the output JSON includes a selfReflection object with prompt and minimumScore"
    - "When --step targets a nonexistent step name, the CLI exits with code 1 and prints an error"
    - "Running compile without --step still works as before (full pipeline JSON output)"
  artifacts:
    - path: "packages/cli/commands/compile.ts"
      provides: "Updated compile command with --step flag handling"
      contains: "compileStep"
    - path: "packages/cli/cli.ts"
      provides: "CLI entry point passing --step value to compile"
      contains: "step"
    - path: "packages/cli/cli.test.ts"
      provides: "Integration tests for --step flag and self-reflection output"
      contains: "--step"
    - path: "packages/cli/fixtures/self-reflection.logic.md"
      provides: "Test fixture with self_verification rubric for testing CLIU-02"
      contains: "self_verification"
  key_links:
    - from: "packages/cli/cli.ts"
      to: "packages/cli/commands/compile.ts"
      via: "options.step passed to runCompile"
      pattern: "step.*options"
    - from: "packages/cli/commands/compile.ts"
      to: "@logic-md/core compileStep"
      via: "import and call compileStep(spec, stepName, context)"
      pattern: "compileStep\\(spec"
---

<objective>
Add --step flag to the compile command so developers can compile a single named step and see its self-reflection prompt when applicable.

Purpose: CLIU-01 and CLIU-02 — single-step compilation with self-reflection output is the final CLI feature for v1.1.
Output: Updated compile command, CLI arg parsing, integration tests, and a self-reflection test fixture.
</objective>

<execution_context>
@/Users/rainierpotgieter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainierpotgieter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/cli/cli.ts
@packages/cli/commands/compile.ts
@packages/cli/cli.test.ts
@packages/core/compiler.ts
@packages/core/types.ts
@packages/core/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — Write failing tests for --step flag and self-reflection output</name>
  <files>packages/cli/cli.test.ts, packages/cli/fixtures/self-reflection.logic.md</files>
  <action>
1. Create `packages/cli/fixtures/self-reflection.logic.md` — a valid LOGIC.md fixture with:
   - spec_version: "1.0", name: "self-reflection-test"
   - reasoning: strategy: cot
   - steps: analyze (with description and instructions), evaluate (needs: [analyze])
   - quality_gates.self_verification: enabled: true, strategy: rubric, rubric with criteria array (e.g., [{name: "accuracy", weight: 0.6, description: "..."}, {name: "completeness", weight: 0.4, description: "..."}]) and minimum_score: 0.7

2. Add a new `describe("compile --step", ...)` block in `packages/cli/cli.test.ts` with these tests:

   a. `it("compiles a single step and outputs JSON with systemPromptSegment")` — run `["compile", VALID_FIXTURE, "--step", "analyze"]`, expect exit 0, parse stdout as JSON, expect `systemPromptSegment` to be a string containing "analyze", expect `metadata.stepName` to be "analyze".

   b. `it("includes selfReflection when step has self-verification enabled")` — run `["compile", SELF_REFLECTION_FIXTURE, "--step", "analyze"]`, expect exit 0, parse stdout as JSON, expect `selfReflection` to be an object with `prompt` (string containing "Self-Evaluation") and `minimumScore` (number, 0.7).

   c. `it("selfReflection is null when no self-verification configured")` — run `["compile", VALID_FIXTURE, "--step", "analyze"]`, expect exit 0, parse stdout as JSON, expect `selfReflection` to be null.

   d. `it("exits 1 when --step names a nonexistent step")` — run `["compile", VALID_FIXTURE, "--step", "nonexistent"]`, expect exit 1, expect stderr to contain "not found".

   e. `it("compile without --step still outputs full pipeline JSON")` — run `["compile", VALID_FIXTURE]`, expect exit 0, parse stdout, expect `name` to be "test-strategy" and `_dagLevels` to exist (existing behavior preserved).

   Add `const SELF_REFLECTION_FIXTURE = resolve(__dirname, "fixtures/self-reflection.logic.md");` at the top alongside existing fixture paths.

3. Run `npm test -- packages/cli/cli.test.ts` — tests a-d MUST fail (RED). Test e should pass (existing behavior).

4. Commit: `test(16-01): add failing tests for --step flag and self-reflection output`
  </action>
  <verify>
    <automated>cd /Users/rainierpotgieter/development/logic-md && npm test -- packages/cli/cli.test.ts 2>&1 | tail -20</automated>
    <manual>New tests fail, existing tests still pass</manual>
  </verify>
  <done>4 new failing tests exist for --step flag behavior. Existing compile tests still pass. Self-reflection fixture exists.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — Implement --step flag in CLI and compile command</name>
  <files>packages/cli/cli.ts, packages/cli/commands/compile.ts</files>
  <action>
1. In `packages/cli/cli.ts`:
   - Add `step: { type: "string" }` to the parseArgs options object.
   - Extract `values.step` and pass it in the options object: `const options = { json: values.json === true, step: values.step as string | undefined };`
   - Update the help text to include `--step <name>` under compile command: `"  compile <file>    Parse, validate, resolve imports, and output JSON"` add line `"    --step <name>   Compile only a specific step (shows self-reflection if enabled)"`

2. In `packages/cli/commands/compile.ts`:
   - Add `step?: string` to the `CommandOptions` interface.
   - Add `import { compileStep } from "@logic-md/core";` (add to existing import).
   - After the existing DAG resolve section (the `if (spec.steps && ...)` block), insert a new branch BEFORE that block: if `options.step` is provided (truthy string), then:
     a. Build an ExecutionContext: `{ currentStep: options.step, previousOutputs: {}, input: null, attemptNumber: 1, branchReason: null, previousFailureReason: null }`
     b. Import `ExecutionContext` type from `@logic-md/core` (add to imports).
     c. Wrap `compileStep(spec, options.step, ctx)` in a try/catch. On `CompilerError`, print `formatError(err.message)` and return 1.
     d. Import `CompilerError` from `@logic-md/core` (add to imports).
     e. Serialize the CompiledStep to JSON for output. The `qualityGates` field contains functions which can't serialize — replace it with a count: in the JSON output, replace `qualityGates` with `qualityGateCount: compiled.qualityGates.length`.
     f. Build a plain object for output: `{ systemPromptSegment, outputSchema, qualityGateCount, selfReflection, retryPolicy, metadata, tokenWarning }` — destructure from the compiled result.
     g. `console.log(JSON.stringify(outputObj, null, 2))` and return 0.
   - The existing full-pipeline code path (no --step) remains unchanged.
   - Update the `_options` parameter name to `options` (remove underscore prefix since it's now used).

3. Rebuild: `npx tsc --build tsconfig.build.json` from repo root.

4. Run `npm test -- packages/cli/cli.test.ts` — all tests MUST pass (GREEN).

5. Commit: `feat(16-01): implement --step flag with self-reflection output in compile command`
  </action>
  <verify>
    <automated>cd /Users/rainierpotgieter/development/logic-md && npx tsc --build tsconfig.build.json && npm test -- packages/cli/cli.test.ts 2>&1 | tail -20</automated>
    <manual>All tests pass including new --step tests</manual>
  </verify>
  <done>All CLI tests pass. `logic-md compile file.logic.md --step analyze` outputs compiled step JSON. Self-reflection prompt appears in output when self-verification is enabled. Nonexistent step returns exit 1. Existing compile behavior unchanged.</done>
</task>

</tasks>

<verification>
1. `npm test` — full test suite passes (no regressions)
2. `npx tsc --build tsconfig.build.json` — no type errors
3. Manual: `node packages/cli/dist/cli.js compile packages/cli/fixtures/valid.logic.md --step analyze` outputs JSON with systemPromptSegment
4. Manual: `node packages/cli/dist/cli.js compile packages/cli/fixtures/self-reflection.logic.md --step analyze` outputs JSON with selfReflection.prompt containing rubric criteria
</verification>

<success_criteria>
- `logic-md compile file.logic.md --step analyze` outputs compiled step JSON (CLIU-01)
- Self-reflection prompt included in output when self_verification is enabled (CLIU-02)
- Nonexistent step name exits 1 with clear error
- Existing compile command (no --step) works unchanged
- All tests pass, TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/16-cli-compile-step/16-01-SUMMARY.md`
</output>
