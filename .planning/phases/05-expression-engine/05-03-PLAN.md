---
phase: 05-expression-engine
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - packages/core/index.ts
  - packages/core/expression.test.ts
autonomous: true
requirements:
  - EXPR-01
  - EXPR-08

must_haves:
  truths:
    - "evaluate and ExpressionContext are importable from packages/core barrel export"
    - "ExpressionError is importable from packages/core barrel export"
    - "Integration-style tests with realistic LOGIC.md expressions all pass"
    - "No eval() or Function constructor exists in any core module"
    - "All existing tests (parser, validator) still pass"
  artifacts:
    - path: "packages/core/index.ts"
      provides: "Barrel export including expression engine public API"
      exports: ["evaluate", "ExpressionContext", "ExpressionError"]
    - path: "packages/core/expression.test.ts"
      provides: "Integration tests with realistic LOGIC.md expression patterns"
      contains: "describe.*integration"
  key_links:
    - from: "packages/core/index.ts"
      to: "packages/core/expression.ts"
      via: "Re-export of evaluate, ExpressionContext, ExpressionError"
      pattern: "from.*expression"
---

<objective>
Wire the expression engine into the package barrel export and add integration tests using realistic LOGIC.md expression patterns from the specification.

Purpose: Make the expression engine usable by downstream consumers (CLI, import resolver) and verify it handles real-world expression patterns from the LOGIC.md spec.
Output: Updated `packages/core/index.ts` with expression exports. Integration test suite in expression.test.ts.
</objective>

<execution_context>
@/Users/rainierpotgieter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainierpotgieter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-expression-engine/05-02-SUMMARY.md
@packages/core/index.ts
@packages/core/expression.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Barrel export and integration tests</name>
  <files>packages/core/index.ts, packages/core/expression.test.ts</files>
  <action>
**Update packages/core/index.ts:**
Add expression engine exports following the existing pattern:
```typescript
export { evaluate, ExpressionError, type ExpressionContext } from "./expression.js";
```

**Add integration tests to packages/core/expression.test.ts:**

Add a `describe("integration -- LOGIC.md expressions")` block with tests that mimic real LOGIC.md usage patterns:

1. **Quality gate check:** `{{ output.findings.length > 0 }}` with context `{ output: { findings: ["issue1", "issue2"] } }` -> `true`

2. **Step verification:** `{{ steps.analyze.output.confidence >= 0.8 }}` with context `{ steps: { analyze: { output: { confidence: 0.92 } } } }` -> `true`

3. **Branch condition:** `{{ output.risk_level == "high" && output.findings.length > 3 }}` with context `{ output: { risk_level: "high", findings: [1,2,3,4] } }` -> `true`

4. **Ternary routing:** `{{ output.score > 80 ? "pass" : "review" }}` with context `{ output: { score: 65 } }` -> `"review"`

5. **Array contains check:** `{{ output.tags.contains("critical") }}` with context `{ output: { tags: ["warning", "critical", "info"] } }` -> `true`

6. **Every with property:** `{{ steps.validate.output.checks.every("passed") }}` with context `{ steps: { validate: { output: { checks: [{ passed: true }, { passed: true }] } } } }` -> `true`

7. **Some with property:** `{{ output.results.some("hasError") }}` with context `{ output: { results: [{ hasError: false }, { hasError: true }] } }` -> `true`

8. **Negation:** `{{ !output.approved }}` with context `{ output: { approved: false } }` -> `true`

9. **Complex combined:** `{{ output.findings.length > 0 || !steps.review.output.approved }}` with appropriate context

10. **Escalation trigger:** `{{ steps.analyze.output.confidence < 0.5 }}` with context `{ steps: { analyze: { output: { confidence: 0.3 } } } }` -> `true`

Also add a test that verifies: `grep -r "eval(" packages/core/expression.ts` and `grep -r "new Function" packages/core/expression.ts` find nothing. This can be a test that imports the file content and checks it does not contain these strings, or simply a note to verify manually.
  </action>
  <verify>
    <automated>cd /Users/rainierpotgieter/development/logic-md && npx vitest run && npx tsc --noEmit -p packages/core/tsconfig.json</automated>
    <manual>All tests pass including integration tests. No type errors. Barrel exports work.</manual>
  </verify>
  <done>evaluate, ExpressionContext, and ExpressionError are exported from packages/core. 10+ integration tests with realistic LOGIC.md expression patterns all pass. All existing tests (parser, validator, schema) continue to pass. No eval() or Function constructor in codebase.</done>
</task>

</tasks>

<verification>
- `npx vitest run` -- ALL tests pass (expression + parser + validator + schema)
- `npx tsc --noEmit -p packages/core/tsconfig.json` -- no type errors
- `grep -rn "eval(\|new Function" packages/core/expression.ts` -- zero matches
- Import test: `import { evaluate } from "@logic-md/core"` resolves correctly
</verification>

<success_criteria>
1. evaluate, ExpressionContext, ExpressionError exported from packages/core barrel
2. 10+ integration tests pass with realistic LOGIC.md expression patterns
3. All existing tests continue to pass (no regressions)
4. Zero eval() or Function constructor usage confirmed
5. TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-expression-engine/05-03-SUMMARY.md`
</output>
